---
import { TESTIMONIALS } from "../constants/site"
---

<section id="depoimentos" class="w-full bg-primary" aria-label="Depoimentos de clientes">
  <div class="mx-auto max-w-[1200px] flex flex-col items-center gap-6 px-6 md:px-10 lg:px-16 py-12">
    <div class="reveal flex items-center gap-3" data-reveal>
      <span class="block h-0.5 w-8 bg-accent-dark"></span>
      <span class="font-body text-xs font-medium text-accent-dark tracking-[1.2px] uppercase">Depoimentos</span>
      <span class="block h-0.5 w-8 bg-accent-dark"></span>
    </div>

    <h3 class="reveal text-center font-heading text-2xl font-medium text-accent-light" data-reveal>O que nossos clientes dizem</h3>

    <div class="reveal-stagger grid w-full grid-cols-3 gap-4 max-md:grid-cols-1" data-reveal>
      {TESTIMONIALS.map((t) => (
        <div class="reveal flex flex-col gap-3 rounded-xl border border-border-dark/50 bg-dark-card p-5 transition-all duration-300 hover:scale-[1.02] hover:shadow-xl hover:border-accent/20" data-spotlight>
          <span class="font-body text-sm text-accent-dark">&starf;&starf;&starf;&starf;&starf;</span>
          <p class="font-body text-[13px] italic text-accent-light/75 leading-relaxed">&ldquo;{t.quote}&rdquo;</p>
          <span class="block h-px w-full bg-border-dark"></span>
          <div class="flex items-center gap-2">
            <div class="flex h-8 w-8 items-center justify-center rounded-full bg-border-dark">
              <span class="font-body text-xs font-medium text-accent-light">{t.initials}</span>
            </div>
            <div class="flex flex-col">
              <span class="font-body text-[13px] font-medium text-accent-light">{t.name}</span>
              <span class="font-body text-[11px] text-accent-light/60">Cliente</span>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
